# Optional: required only if you want to include X in the feed.
# Create a developer app at https://developer.x.com and copy your Bearer Token.
X_BEARER_TOKEN=your_x_bearer_token_here

# Optional: local Ollama endpoint.
OLLAMA_HOST=http://127.0.0.1:11434

# Optional: default analysis model for CLI runs.
OLLAMA_MODEL=gpt-oss:120b
